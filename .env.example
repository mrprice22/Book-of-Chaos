# Client → SpacetimeDB connection. Copy to client/.env.local to override the
# defaults, which target the local standalone instance.
VITE_SPACETIME_URI=ws://localhost:3000
VITE_SPACETIME_DB_NAME=book-of-chaos
